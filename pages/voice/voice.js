/**Template.voice.events({
  'click button'(element,instance){
      jQuery.getJSON(url, function(data){
        response = data.results[0].fulfillment.speech;
        console.log(response);
      });
  }
});
*/
